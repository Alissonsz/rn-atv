import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textInput: {
    borderColor: 'black',
    borderWidth: 2,
    borderRadius: 8,
    width: '80%',
    marginBottom: 8,
    paddingLeft: 8
  },
});

export default styles;
